# Scaffold New Section

I need to scaffold a new section in the product.

Please help me create the necessary files and directories for a new section named `{{section_name}}` (replace with actual name).

The files to be created are:

1.  `product/sections/{{section_name}}/spec.md`:
    - Should contain the specification for the section.
    - Headers: Title, Goal, Core Features, User Flows, Data Requirements.
2.  `product/sections/{{section_name}}/data.json`:
    - Should contain sample data for the screen designs.
3.  `product/sections/{{section_name}}/types.ts`:
    - Should contain TypeScript interfaces for the data.
4.  `src/sections/{{section_name}}/components/index.ts`:
    - Export file for components.
5.  `src/sections/{{section_name}}/ViewName.tsx`:
    - A placeholder view component.

Please ask me for the name of the section if I haven't provided it, and then proceed to create these files with initial boilerplate content.
